a = int(input("Enter your age; "))
print("your age is" , a)
print(a > 18)
print(a<=18)
print(a==18)
print(a!=18)
if(a>18):
 print("you can do it")
else:
  print("you cant do it")

output: Enter your age; 8 
your age is 8
False
True
False
True
you cant do it

dat 1 :_ for i in range(1, 4):
    print("\nStudent", i)
    percentage = float(input("Enter percentage: "))

    if percentage >= 90:
        grade = "A+"
    else:
        if percentage >= 80:
            grade = "A"
        else:
            if percentage >= 70:
                grade = "B+"
            else:
                if percentage >= 60:
                    grade = "B"
                else:
                    if percentage >= 50:
                        grade = "C"
                    else:
                        if percentage >= 40:
                            grade = "D"
                        else:
                            grade = "F"

    print("Grade:", grade)
 output :-Student 1
Enter percentage: 86
Grade: A

Student 2
Enter percentage: 56
Grade: C

Student 3
Enter percentage: 30
Grade: F

DAY2 :- 
for i in range(1, 3):
    print(f"\nStudent {i}")
    
    name = input("Enter student name: ")
    
    percentage = float(input("Enter percentage marks: "))

    
    if percentage >= 40:
        grade = "Pass"
    else:
        grade = "Fail"

    print(f"{name} has scored {percentage}% → {grade}")

output:- Student 1
Enter student name: EWS
Enter percentage marks: 88
EWS has scored 88.0% → Pass

Student 2
Enter student name: BOB
Enter percentage marks: 33
BOB has scored 33.0% → Fail

DAY:-3 for i in range(2):
    name = input("Enter student name: ")
    marks = float(input("Enter percentage marks: "))

    if marks >= 90:
        grade = "A+"
    elif marks >= 80:
        grade = "A"
    elif marks >= 70:
        grade = "B+"
    elif marks >= 60:
        grade = "B"

output-Enter student name: wee
Enter percentage marks: 88
Enter student name: wee
Enter percentage marks: 35

DAY4:- for i in range(2):
    name = input(f"\nEnter name of student {i+1}: ")

    
    while True:
        try:
            marks = float(input("Enter percentage marks (0-100): "))
            if 0 <= marks <= 100:
                break
            else:
                print("Error: Marks must be between 0 and 100. Try again.")
        except ValueError:
            print("Error: Please enter a valid number.")

    if marks >= 90:
        grade = "A+"
    else:
        if marks >= 80:
            grade = "A"
        else:
            if marks >= 70:
                grade = "B+"
            else:
                if marks >= 60:
                    grade = "B"
                else:
                    if marks >= 50:
                        grade = "C"
                    else:
                        if marks >= 40:
                            grade = "D"
                        else:
                            grade = "F"

    
    print(f"{name} has scored {marks}% → Grade: {grade}")

 output:- Enter name of student 1: EWS
Enter percentage marks (0-100): 103
Error: Marks must be between 0 and 100. Try again.
Enter percentage marks (0-100): Try again
Error: Please enter a valid number.
Enter percentage marks (0-100): 88
EWS has scored 88.0% → Grade: A

Enter name of student 2: BOB
Enter percentage marks (0-100): twenty
Error: Please enter a valid number.
Enter percentage marks (0-100): 20
BOB has scored 20.0% → Grade: F

day5:- def get_marks(name):
    while True:
        try:
            marks = float(input(f"Enter marks for {name} (0-100): "))
            if 0 <= marks <= 100:
                return marks
            else:
                print("Error: Marks must be between 0 and 100.")
        except ValueError:
            print("Error: Please enter a valid number.")

def assign_grade(marks):
    if marks >= 90:
        return "A+"
    else:
        if marks >= 80:
            return "A"
        else:
            if marks >= 70:
                return "B+"
            else:
                if marks >= 60:
                    return "B"
                else:
                    if marks >= 50:
                        return "C"
                    else:
                        if marks >= 40:
                            return "D"
                        else:
                            return "F"

for i in range(2):
    name = input(f"\nEnter name of student {i+1}: ")
    marks = get_marks(name)
    grade = assign_grade(marks)
    print(f"{name} has scored {marks}% → Grade: {grade}")

OUTPUT:-Enter name of student 1: EWS
Enter marks for EWS (0-100): 88
EWS has scored 88.0% → Grade: A

Enter name of student 2: BOB
Enter marks for BOB (0-100): 35
BOB has scored 35.0% → Grade: F
